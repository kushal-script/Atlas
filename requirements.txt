# Runtime dependencies of the inference and generation scripts, pinned to the
# versions they were developed and measured against. This is the file to
# install; a clean clone plus this file runs generate_dataset.py and
# localize.py with no further steps.
# requirements_freeze.txt holds the complete pip freeze of the development
# environment, including the test and training only packages.
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.1
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.18.0
setuptools==78.1.0
six==1.17.0
