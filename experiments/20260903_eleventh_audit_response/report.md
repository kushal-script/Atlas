# Eleventh review: run against an hour old checkout, three new zero risk hardenings taken, the rest already shipped or standing

The eleventh review, static and careful, was produced against a checkout
predating the tenth round's commit: its must fix list, stripping csv paths
and pair ids, correcting the nominal only when weak sentence, exercising
absent pairs and relative paths in the packaging self test, and a strict
rotation sign test, had already shipped, the last as an oracle stronger
than the corner assertions it asked for, built through the organisers' own
warp with no generator code. The corner assertions were added anyway: the
extreme corner test at 8x with plus five degrees and 12x with minus five
now asserts the reported rotation against the injected angle within half a
degree and in sign, and passes.

Three of its new findings were taken, each validated byte identical on the
smoke suite with 72 tests passing:

The error path score inversion was its sharpest catch. A pair rejected
because its file was missing or its processing raised carried score zero,
which ranks a possibly correct rejection below every confident answer in
the calibration column; the degenerate blank path already said 0.5. All
three no evidence paths, timeout, exception and the missing row sweep, now
carry the same neutral 0.5, the honest confidence of a decision made with
no evidence, and the earlier disposition that the distinction had never
touched a scored row is superseded by the better argument rather than
contradicted by it: it still has never touched one, and if a malformed
input hidden test ever does, the ranking now tells the truth.

The shipped model carried a standardization constant of 1e-09 for the one
constant feature, predating the fitting script's floor and inert only
because that weight is exactly zero; the model file now carries the same
floor the fitter enforces, verified identical to the last bit over 216
recorded pairs, maximum probability difference exactly zero.

Pose selection now coerces a non finite correlation peak to a value below
the valid range before it can enter the tried table, so a degenerate
surface can never win max() by NaN comparison order; on clean suites no
such peak exists and the smoke suite is unchanged.

The runtime row in the conventions table now names every number's context,
pooled development, pre pool, serial reference class, and the organisers'
forfeit, so no single median can be quoted without its machine.

Standing, with the record: the combiner override's unreachable margin is
the tenth round's documented decline, and the review's suggested flag flip
would be wrong in a way its no benchmark change note misses, since
disabling the combiner would also silence the rr diagnostics that feed the
presence model; the optical always found rule the review calls an external
assumption is the addendum's own page four, Set D, reference present,
bonus only; the noise weight residual risk is the measured decline of the
final adversarial audit; and the nominal gate, duplicate id semantics,
Windows bound and luminance only optical path stand as measured.
