# Frozen from a Python 3.11 environment, the reference machine version.
# The Phase 1 file pins numpy 2.5.1; resolving these requirements inside a
# real Python 3.11 environment selected 2.4.6 instead, so the Phase 1 pin is
# not what the reference interpreter would install and this file records what
# it actually resolved to. register.py was verified to produce byte identical
# predictions under both 3.11 and 3.14.
numpy==2.4.6
opencv-python==5.0.0.93
pillow==12.3.0
scipy==1.17.1
